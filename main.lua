-- ADRYX OS - Sistema Base
-- Estética: Neon Azul e Preto

local os_name = "ADRYX OS"
local version = "1.0.0"

print("==============================")
print("INICIALIZANDO " .. os_name)
print("VERSAO: " .. version)
print("==============================")

function carregarInterface()
    print("[SISTEMA]: Carregando interface Neon Azul...")
    -- Aqui entrará a lógica visual e os scripts de farm
end

carregarInterface()
print("[STATUS]: Sistema Pronto.")
